#include "holberton.h"
/**
* print_sign - print_signs
* @n : variable input
*Return: signs
*/
int print_sign(int n)
{
if (n > 0)
{
_putchar ('+');
return (1);
}
if (n == 0)
{
_putchar (49);
return (0);
}
if (n < 0)
{
_putchar ('-');
return (-1);
}
}
