#include "holberton.h"
#include <stdio.h>
/**
 * is_prime_number - check the code for Holberton School students.
 * @n : input ..
 * Return: .
 */
int is_prime_number(int n)
{
  if ((n + 1) % 2 == 0 && (n % 2) == 0)
return (0);
else
return (1);
}
