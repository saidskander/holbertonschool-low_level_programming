#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * print_grid - prints a grid of integers
 * @: the address of the two dimensional grid
 * Return: Nothing.
 */
char *_strdup(char *str);
{
  int i;
  char *x;
  for (i = 0; str[i]; i++)
    ;
  *str = malloc (*x * sizeof)
}
